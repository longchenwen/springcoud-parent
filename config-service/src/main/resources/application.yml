server:
  port: 8765
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:

          uri:

          search-paths:
          username:
          password:

      label:
